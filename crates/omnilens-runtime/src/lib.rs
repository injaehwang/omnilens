//! # omnilens-runtime
//!
//! Runtime profiling overlay. Attaches to running processes via
//! eBPF (Linux), ETW (Windows), or DTrace (macOS) and maps runtime
//! behavior back to the semantic graph.

// Phase 3 implementation.
